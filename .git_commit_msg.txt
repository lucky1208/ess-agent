fix(deploy): Plan B round 2 - client-render-svg.js inline 进 index.html

ff28854 commit 把 client-render-svg.js 作为独立 .js 文件部署到
dist/client-render-svg.js,期望浏览器通过 <script src="client-render-svg.js">
加载。但 Vercel CDN 返回 404 NOT_FOUND — 这个独立 .js 没被 serve。

虽然 dist/index.html 被部署了,但其它 dist/ 下的文件路径访问不到
(curl https://www.ess-agent.com/client-render-svg.js 404)。

修复:把 dist/client-render-svg.js (3.1 MB) 的完整内容 inline 进
dist/index.html 和 index.html 的 <script> 块(放在 mermaid 之后)。
HTML 总大小:3.2 MB -> 6.4 MB,首次加载多 2-3 秒,但绝对可用。

工具: tools/inline_client_renderer.js (idempotent,可重跑)
dist/client-render-svg.js 保留在 repo,作为 source-of-truth 和 trace。