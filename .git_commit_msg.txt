fix: 禁用 MiniMax-M3 thinking(过度思考导致 content 为空)

上次改的 budget_tokens=16384 还是不够: 实测 M3 在 36K 字符的 reasoning
里反复迭代拓扑/BOM/坐标(估计算下来 9K+ token), 切不到 text 输出,
content 还是 0, 5级 JSON 修复全部失败.

干脆把 thinking 直接关掉, 让 M3 跳过思考阶段直接出 JSON.
牺牲「思考过程实时显示」特性, 换取功能可用.

Anthropic 协议层:
- thinking.type: enabled + budget_tokens → disabled
- response.content 数组里就只有 text 块, 没有 thinking 块
- 后端 handleAnthropicProvider 转换代码兼容(只 extract text 块)
- 前端 callLLM 直接读 message.content 拿 JSON, 不需要 reasoning_content

如果用户强烈需要看思考过程, 后续可以加 query 参数单独控制
"某个流程禁用 thinking, 其他流程保留". 当前先解决功能可用性.
