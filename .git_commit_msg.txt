fix(deploy): Vercel function bundle 直接含 IEC 库 (基于 2a87f6e)

affd8b4 + 2a87f6e 后,Vercel 部署后 L5 SLD 仍渲染为 5 个空矩形。
排查后根因:vercel.json 的 includeFiles="iec_symbols_svg/**" 在当前
Vercel 版本下没把库打包进 function bundle。

修复:把 iec_symbols_svg/ 复制到 api/_iec_symbols_svg/ — Vercel 自动
把整个 api/ 树打进 function bundle,无需依赖 includeFiles。

改动:
1. iec_symbols_svg/ -> api/_iec_symbols_svg/ (373 SVG + index.json)
2. dist/_iec_symbols_svg/ 同步副本 (静态资源兜底)
3. api/render_svg.js resolveIecDir() 加 __dirname 候选路径
   (path.join(__dirname, '_iec_symbols_svg') 优先),console.log
   输出 [resolveIecDir] cwd / 候选数 / 命中路径,Vercel 函数日志可查

预期:Vercel 部署后 L5 SLD 不再 fallback 到 placeholder rectangle,
应输出 75KB SVG,含 5 个真 IEC 矢量符号 (断路器/变压器/电池/PCS/电网)。

本地验证:
  smoke svg_len=75813, symbols=5, layers=6, components=7